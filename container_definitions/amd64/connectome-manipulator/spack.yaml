---
spack:
  specs: [parquet-converters, py-connectome-manipulator]
  concretizer:
    unify: when_possible
  packages:
    py-numpy-quaternion:
      require: ~numba~scipy
    boost:
      variants: cxxstd=11
    all:
      providers:
        mpi: [mpich]

