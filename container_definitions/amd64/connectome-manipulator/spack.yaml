---
spack:
  specs: [parquet-converters, py-connectome-manipulator]
  concretizer:
    unify: when_possible
  packages:
    py-numpy-quaternion:
      require: ~numba~scipy  # See above, get around Numba
    boost:
      variants: cxxstd=11
    all:
      providers:
        mpi: [mpich]

